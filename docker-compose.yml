version: "2"
services:
  web:
    image: photo-gallery
    ports:
    - "80:3000"
    depends_on:
    - mongo
    volumes:
      - .:/code
  mongo:
    image: mongo
    ports:
    - "27017:27017"
    volumes:
      - data-volume:/data/db
volumes:
  data-volume: